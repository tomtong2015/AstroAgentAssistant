version: 0.9.0
inputs:
  files:
    - run.sh
workflow:
  type: serial
  specification:
    steps:
      - name: run-bash
        environment: gitlab-p4n.aip.de:5005/p4nreana/reana-env:py311-astro-ml.2891a60c
        kubernetes_memory_limit: "32Gi"
        kubernetes_job_timeout: 7200
        commands:
          - bash -lc 'cd "$REANA_WORKSPACE" && bash run.sh'
        outputs:
          files:
            - output.txt
outputs:
  files:
    - output.txt
